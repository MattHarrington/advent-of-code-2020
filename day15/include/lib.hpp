#pragma once

#include <vector>

int play_game_map_to_vector(int, const std::vector<int>&);

int play_game_map_to_size_t(int, const std::vector<int>&);

int play_game_with_vectors(int, const std::vector<int>&);