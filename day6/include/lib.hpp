#pragma once

#include <string>
#include <vector>

std::vector<std::string> read_input(const std::string&);

size_t part1(const std::vector<std::string>&);

size_t part2(const std::vector<std::string>&);
