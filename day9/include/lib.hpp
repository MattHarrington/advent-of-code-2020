#pragma once

#include <vector>

std::vector<long long> read_input(const std::string&);

bool is_valid_xmas_number(const long long, const std::vector<long long>&);

long long part1(const long long, const std::vector<long long>&);

long long part2(const long long, const std::vector<long long>&);
